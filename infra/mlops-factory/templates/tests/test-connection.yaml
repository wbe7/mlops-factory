apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mlops-factory.fullname" . }}-test-connection"
  labels:
    {{- include "mlops-factory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mlops-factory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
