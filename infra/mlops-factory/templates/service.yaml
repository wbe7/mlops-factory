apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlops-factory.fullname" . }}
  labels:
    {{- include "mlops-factory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mlops-factory.selectorLabels" . | nindent 4 }}
